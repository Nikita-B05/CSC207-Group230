package interface_adapter.game_over;

public class GameOverController {
/*
    private final JFrame frame;
    private final GameEndState gameEndState; // GameState to hold game outcome info
    private final Object[][] leaderboardData; // Your leaderboard data
    private final Object[] currentUserData; // Data for the current player

    public GameController
            (JFrame frame, GameEndState gameEndState, Object[][] leaderboardData, Object[] currentUserData) {
        this.frame = frame;
        this.gameEndState = gameEndState;
        this.leaderboardData = leaderboardData;
        this.currentUserData = currentUserData;
    }
    // Method to start/end the game based on its state
    public void endGame() {
        // Check if game is successful or over
        if (gameEndState.isGameOver()) {
            switchToGameOverView(); // Show the Game Over View
        } else {
            switchToGameSuccessView(); // Show the Game Success View
        }
    }

    private void switchToGameOverView() {
        GameOverView overView = new GameOverView();
        showView(overView);
    }

    private void switchToGameSuccessView() {
        GameSuccessView successView = new GameSuccessView(leaderboardData, currentUserData);
        showView(successView);
    }

    // Show the Game Success View
    private void switchToGameEndSuccessView() {
        GameSuccessView successView = new GameSuccessView(leaderboardData, currentUserData);
        showView(GameSuccessView);
    }

    // Show the Game Over View
    private void switchToGameEndOverView() {
        GameOverView overView = new GameOverView();
        showView(GameOverView);
    }

    // Utility method to set the frame content to a specific view
    private void showView(JPanel view) {
        frame.getContentPane().removeAll();
        frame.add(view);
        frame.revalidate();
        frame.repaint();
    }

 */
}
